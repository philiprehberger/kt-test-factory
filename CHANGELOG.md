# Changelog

## [0.1.0] - 2026-03-18

### Added

- `factory<T> {}` DSL for defining test data factories

- `build()`, `build(override)`, `buildList(n)`

- Named traits for presets

- `sequence {}` for auto-incrementing values

- `Faker` for random realistic data
